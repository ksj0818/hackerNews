// 타입스크립트는 유형으로 만들어 낼 수 있음
type Centimeter = number;
type RainbowColor = 'red' | 'orenge' | 'yellow' | 'green' | 'blue' | 'indigo' | 'violet'

let age = 10;
let weight: number = 190;
let height: Centimeter = 123; // 타입 정의만으로도 이렇게 알려줄 수 있다
let color: RainbowColor = 'violet';